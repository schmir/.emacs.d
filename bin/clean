#! /usr/bin/env bash
# Remove generated/temp files, cached/package dirs, and interactively git clean.

set -euo pipefail
root=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" &>/dev/null && git rev-parse --show-toplevel)
cd -- "${root}"
rm -f lisp/loaddefs.el lisp/.auto-site.el ./result \#*\# .\#*
rm -rf var/elpa-gnupg var/elpa-packages var/eln-cache auto-save-list
exec git clean -e etc -e var -e .jj -xfdi
